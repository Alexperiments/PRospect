# PRospect Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build PRospect — a Cloudflare Worker + static HTML frontend that finds approachable open-source contribution issues by fetching GitHub issues, filtering out actively-worked ones, and ranking the rest via Claude.

**Architecture:** A single Cloudflare Worker serves `index.html` (via Workers Sites / KV) for `GET /` and exposes `POST /api/analyze` for the analysis pipeline. The analyzer fetches GitHub issues/PRs/docs in parallel, filters available issues, calls Claude, and returns a JSON object with stats and ranked issues. Pure logic (URL parsing, issue filtering) lives in separate modules for testability.

**Tech Stack:** Cloudflare Workers (TypeScript, Wrangler v3), `@cloudflare/kv-asset-handler` (Workers Sites), `@cloudflare/workers-types`, Vitest (unit tests for pure logic), GitHub REST API v3, Anthropic API (`claude-sonnet-4-5`)

---

## File Map

| File | Responsibility |
|------|---------------|
| `worker/src/types.ts` | Shared TS interfaces (Env, GitHubIssue, GitHubPR, RankedIssue, AnalyzeResponse) |
| `worker/src/parseRepo.ts` | Pure: normalize GitHub URL/slug → "owner/repo" or null |
| `worker/src/filterIssues.ts` | Pure: filter issues by assignees and PR references |
| `worker/src/analyzer.ts` | GitHub fetching, Anthropic API call, analyzeRepo orchestrator |
| `worker/src/index.ts` | Worker entry: routing, CORS, asset serving, error handling |
| `worker/src/manifest.d.ts` | Type declaration for `__STATIC_CONTENT_MANIFEST` module |
| `worker/test/parseRepo.test.ts` | Vitest unit tests for parseRepo |
| `worker/test/filterIssues.test.ts` | Vitest unit tests for filterAvailableIssues |
| `worker/package.json` | Dependencies + scripts |
| `worker/tsconfig.json` | TypeScript config for Workers |
| `worker/wrangler.toml` | Wrangler config with [site] bucket |
| `worker/vitest.config.ts` | Vitest config (node environment, tests in test/) |
| `static/index.html` | Full frontend: HTML + CSS + JS, single file |
| `.github/workflows/deploy.yml` | CI/CD: deploy on push to main |
| `README.md` | Setup instructions |

---

### Task 1: Project scaffold

**Files:**
- Create: `worker/package.json`
- Create: `worker/tsconfig.json`
- Create: `worker/wrangler.toml`
- Create: `worker/vitest.config.ts`
- Create: `worker/src/manifest.d.ts`

- [ ] **Step 1: Create directory structure**

```bash
mkdir -p worker/src worker/test static .github/workflows
```

Expected: directories created, no error.

- [ ] **Step 2: Create `worker/package.json`**

```json
{
  "name": "prospect-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4",
    "@cloudflare/workers-types": "^4.20240806.0",
    "typescript": "^5.5.3",
    "vitest": "^1.6.0",
    "wrangler": "^3.65.1"
  }
}
```

- [ ] **Step 3: Create `worker/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "types": ["@cloudflare/workers-types"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*.ts"]
}
```

- [ ] **Step 4: Create `worker/wrangler.toml`**

```toml
name = "prospect"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[site]
bucket = "../static"
```

- [ ] **Step 5: Create `worker/vitest.config.ts`**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['test/**/*.test.ts'],
  },
});
```

- [ ] **Step 6: Create `worker/src/manifest.d.ts`**

This declares the special Wrangler module so TypeScript doesn't error on the import.

```typescript
declare module '__STATIC_CONTENT_MANIFEST' {
  const content: string;
  export default content;
}
```

- [ ] **Step 7: Install dependencies**

```bash
cd worker && npm install
```

Expected: `node_modules/` created, no errors.

- [ ] **Step 8: Commit**

```bash
git add worker/ static/ .github/
git commit -m "chore: project scaffold — Cloudflare Worker + vitest setup"
```

---

### Task 2: Shared TypeScript types

**Files:**
- Create: `worker/src/types.ts`

- [ ] **Step 1: Create `worker/src/types.ts`**

```typescript
export interface Env {
  ANTHROPIC_API_KEY: string;
  __STATIC_CONTENT: KVNamespace;
}

export interface GitHubIssue {
  number: number;
  title: string;
  body: string | null;
  assignees: { login: string }[];
  pull_request?: { url: string };
  comments: number;
  html_url: string;
  created_at: string;
}

export interface GitHubPR {
  number: number;
  title: string;
  body: string | null;
}

export interface RankedIssue {
  number: number;
  title: string;
  comments: number;
  html_url: string;
  difficulty: 'easy' | 'medium' | 'hard';
  reason: string;
  tags: string[];
}

export interface AnalyzeResponse {
  stats: {
    openIssues: number;
    available: number;
    openPRs: number;
    ranked: number;
  };
  issues: RankedIssue[];
}
```

- [ ] **Step 2: Commit**

```bash
git add worker/src/types.ts
git commit -m "feat: add shared TypeScript types"
```

---

### Task 3: Input parser — TDD

**Files:**
- Create: `worker/src/parseRepo.ts`
- Create: `worker/test/parseRepo.test.ts`

- [ ] **Step 1: Write the failing tests**

Create `worker/test/parseRepo.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { parseRepo } from '../src/parseRepo';

describe('parseRepo', () => {
  it('accepts bare owner/repo slug', () => {
    expect(parseRepo('pytorch/pytorch')).toBe('pytorch/pytorch');
  });

  it('accepts HTTPS URL', () => {
    expect(parseRepo('https://github.com/pytorch/pytorch')).toBe('pytorch/pytorch');
  });

  it('accepts HTTPS URL with .git suffix', () => {
    expect(parseRepo('https://github.com/pytorch/pytorch.git')).toBe('pytorch/pytorch');
  });

  it('accepts SSH URL git@github.com', () => {
    expect(parseRepo('git@github.com:pytorch/pytorch.git')).toBe('pytorch/pytorch');
  });

  it('accepts org-prefixed SSH URL', () => {
    expect(parseRepo('org-21003710@github.com:pytorch/pytorch.git')).toBe('pytorch/pytorch');
  });

  it('strips trailing path segments after owner/repo', () => {
    expect(parseRepo('https://github.com/pytorch/pytorch/issues/1234')).toBe('pytorch/pytorch');
  });

  it('strips query strings', () => {
    expect(parseRepo('https://github.com/pytorch/pytorch?tab=issues')).toBe('pytorch/pytorch');
  });

  it('strips fragments', () => {
    expect(parseRepo('https://github.com/pytorch/pytorch#readme')).toBe('pytorch/pytorch');
  });

  it('returns null for empty string', () => {
    expect(parseRepo('')).toBeNull();
  });

  it('returns null for whitespace only', () => {
    expect(parseRepo('   ')).toBeNull();
  });

  it('returns null for non-GitHub URL', () => {
    expect(parseRepo('https://gitlab.com/pytorch/pytorch')).toBeNull();
  });

  it('returns null for plain word', () => {
    expect(parseRepo('notarepo')).toBeNull();
  });

  it('handles slugs with dots and dashes', () => {
    expect(parseRepo('astral-sh/uv')).toBe('astral-sh/uv');
  });
});
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
cd worker && npx vitest run test/parseRepo.test.ts
```

Expected: `FAIL` — `Cannot find module '../src/parseRepo'`.

- [ ] **Step 3: Implement `worker/src/parseRepo.ts`**

```typescript
/**
 * Normalizes various GitHub repo input formats to "owner/repo".
 * Returns null if the input cannot be parsed.
 *
 * Accepted formats:
 *   - Bare slug:       pytorch/pytorch
 *   - HTTPS URL:       https://github.com/pytorch/pytorch[.git]
 *   - SSH URL:         git@github.com:pytorch/pytorch.git
 *   - Org SSH:         org-21003710@github.com:pytorch/pytorch.git
 *   - URLs with paths/query/fragment: stripped to owner/repo
 */
export function parseRepo(input: string): string | null {
  const trimmed = input.trim();
  if (!trimmed) return null;

  // Strip .git suffix before matching
  const stripped = trimmed.replace(/\.git$/, '');

  // Match github.com/{owner}/{repo} or github.com:{owner}/{repo}
  const match = stripped.match(
    /github\.com[:/]([a-zA-Z0-9_.\-]+\/[a-zA-Z0-9_.\-]+)/,
  );
  if (match && match[1]) {
    // Take only the first two segments (owner/repo), discard trailing path
    const parts = match[1].split('/');
    if (parts[0] && parts[1]) {
      return `${parts[0]}/${parts[1]}`;
    }
  }

  // Check if already a bare owner/repo slug
  if (/^[a-zA-Z0-9_.\-]+\/[a-zA-Z0-9_.\-]+$/.test(trimmed)) {
    return trimmed;
  }

  return null;
}
```

- [ ] **Step 4: Run tests — verify they pass**

```bash
cd worker && npx vitest run test/parseRepo.test.ts
```

Expected: all 13 tests `PASS`.

- [ ] **Step 5: Commit**

```bash
git add worker/src/parseRepo.ts worker/test/parseRepo.test.ts
git commit -m "feat: add parseRepo with full URL normalization (TDD)"
```

---

### Task 4: Issue filter — TDD

**Files:**
- Create: `worker/src/filterIssues.ts`
- Create: `worker/test/filterIssues.test.ts`

- [ ] **Step 1: Write the failing tests**

Create `worker/test/filterIssues.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { filterAvailableIssues } from '../src/filterIssues';
import type { GitHubIssue, GitHubPR } from '../src/types';

function makeIssue(overrides: Partial<GitHubIssue> & { number: number }): GitHubIssue {
  return {
    title: `Issue ${overrides.number}`,
    body: null,
    assignees: [],
    comments: 0,
    html_url: `https://github.com/owner/repo/issues/${overrides.number}`,
    created_at: '2024-01-01T00:00:00Z',
    ...overrides,
  };
}

function makePR(overrides: Partial<GitHubPR> & { number: number }): GitHubPR {
  return {
    title: `PR ${overrides.number}`,
    body: null,
    ...overrides,
  };
}

describe('filterAvailableIssues', () => {
  it('returns all issues when no PRs exist and no assignees', () => {
    const issues = [makeIssue({ number: 1 }), makeIssue({ number: 2 })];
    expect(filterAvailableIssues(issues, [])).toHaveLength(2);
  });

  it('excludes issues with at least one assignee', () => {
    const issues = [
      makeIssue({ number: 1, assignees: [{ login: 'alice' }] }),
      makeIssue({ number: 2 }),
    ];
    const result = filterAvailableIssues(issues, []);
    expect(result).toHaveLength(1);
    expect(result[0]!.number).toBe(2);
  });

  it('excludes issues referenced by #NNN in a PR title', () => {
    const issues = [makeIssue({ number: 42 }), makeIssue({ number: 99 })];
    const prs = [makePR({ number: 100, title: 'Fix #42' })];
    const result = filterAvailableIssues(issues, prs);
    expect(result).toHaveLength(1);
    expect(result[0]!.number).toBe(99);
  });

  it('excludes issues referenced by #NNN in a PR body', () => {
    const issues = [makeIssue({ number: 42 }), makeIssue({ number: 99 })];
    const prs = [makePR({ number: 100, body: 'Closes #42 and fixes stuff' })];
    const result = filterAvailableIssues(issues, prs);
    expect(result).toHaveLength(1);
    expect(result[0]!.number).toBe(99);
  });

  it('handles PRs with null body without throwing', () => {
    const issues = [makeIssue({ number: 1 })];
    const prs = [makePR({ number: 2, body: null })];
    expect(() => filterAvailableIssues(issues, prs)).not.toThrow();
    expect(filterAvailableIssues(issues, prs)).toHaveLength(1);
  });

  it('excludes issues that are both assigned and referenced', () => {
    const issues = [makeIssue({ number: 5, assignees: [{ login: 'bob' }] })];
    const prs = [makePR({ number: 6, title: 'Fix #5' })];
    expect(filterAvailableIssues(issues, prs)).toHaveLength(0);
  });

  it('handles a PR referencing multiple issues', () => {
    const issues = [makeIssue({ number: 10 }), makeIssue({ number: 20 }), makeIssue({ number: 30 })];
    const prs = [makePR({ number: 99, body: 'Fixes #10 and #20' })];
    const result = filterAvailableIssues(issues, prs);
    expect(result).toHaveLength(1);
    expect(result[0]!.number).toBe(30);
  });

  it('returns empty array when all issues are filtered', () => {
    const issues = [makeIssue({ number: 1, assignees: [{ login: 'x' }] })];
    expect(filterAvailableIssues(issues, [])).toHaveLength(0);
  });
});
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
cd worker && npx vitest run test/filterIssues.test.ts
```

Expected: `FAIL` — `Cannot find module '../src/filterIssues'`.

- [ ] **Step 3: Implement `worker/src/filterIssues.ts`**

```typescript
import type { GitHubIssue, GitHubPR } from './types';

/**
 * Returns issues that:
 *   (a) have zero assignees, AND
 *   (b) whose number does not appear as #NNN in any open PR's title or body.
 */
export function filterAvailableIssues(
  issues: GitHubIssue[],
  prs: GitHubPR[],
): GitHubIssue[] {
  const referenced = new Set<number>();

  for (const pr of prs) {
    const text = `${pr.title ?? ''} ${pr.body ?? ''}`;
    for (const m of text.matchAll(/#(\d+)/g)) {
      referenced.add(Number(m[1]));
    }
  }

  return issues.filter(
    (issue) => issue.assignees.length === 0 && !referenced.has(issue.number),
  );
}
```

- [ ] **Step 4: Run tests — verify they pass**

```bash
cd worker && npx vitest run test/filterIssues.test.ts
```

Expected: all 8 tests `PASS`.

- [ ] **Step 5: Run all tests**

```bash
cd worker && npm test
```

Expected: 21 tests pass across both test files.

- [ ] **Step 6: Commit**

```bash
git add worker/src/filterIssues.ts worker/test/filterIssues.test.ts
git commit -m "feat: add filterAvailableIssues with PR cross-reference logic (TDD)"
```

---

### Task 5: GitHub data fetcher

**Files:**
- Create: `worker/src/analyzer.ts` (first half — fetching only)

- [ ] **Step 1: Create `worker/src/analyzer.ts` with `fetchRepoData`**

```typescript
import type { Env, GitHubIssue, GitHubPR, RankedIssue, AnalyzeResponse } from './types';
import { filterAvailableIssues } from './filterIssues';

const GITHUB_API = 'https://api.github.com';

export class RateLimitError extends Error {
  constructor() {
    super('GitHub rate limit hit. Wait a minute and try again.');
    this.name = 'RateLimitError';
  }
}

async function fetchGitHubJSON<T>(url: string): Promise<T> {
  const res = await fetch(url, {
    headers: {
      'User-Agent': 'PRospect/1.0',
      Accept: 'application/vnd.github+json',
    },
  });
  if (res.status === 403 || res.status === 429) {
    throw new RateLimitError();
  }
  if (!res.ok) {
    throw new Error(`GitHub API ${res.status} for ${url}`);
  }
  return res.json() as Promise<T>;
}

interface RepoData {
  issues: GitHubIssue[];
  prs: GitHubPR[];
  readme: string;
  contributing: string;
}

async function fetchRepoData(owner: string, repo: string): Promise<RepoData> {
  const base = `${GITHUB_API}/repos/${owner}/${repo}`;

  // Issues, PRs, and README all fetched in parallel
  const [issuesRaw, prs, readmeData] = await Promise.all([
    fetchGitHubJSON<GitHubIssue[]>(`${base}/issues?state=open&per_page=100`),
    fetchGitHubJSON<GitHubPR[]>(`${base}/pulls?state=open&per_page=100`),
    fetchGitHubJSON<{ content: string }>(`${base}/readme`).catch(() => ({ content: '' })),
  ]);

  // GitHub's /issues endpoint also returns PRs — strip them out
  const issues = issuesRaw.filter((i) => !i.pull_request);

  const readme = readmeData.content
    ? atob(readmeData.content.replace(/\n/g, ''))
    : '';

  // Try root CONTRIBUTING.md, fall back to docs/CONTRIBUTING.md
  let contributing = '';
  try {
    const data = await fetchGitHubJSON<{ content: string }>(
      `${base}/contents/CONTRIBUTING.md`,
    );
    contributing = atob(data.content.replace(/\n/g, ''));
  } catch {
    try {
      const data = await fetchGitHubJSON<{ content: string }>(
        `${base}/contents/docs/CONTRIBUTING.md`,
      );
      contributing = atob(data.content.replace(/\n/g, ''));
    } catch {
      // No CONTRIBUTING.md — that's fine, proceed without it
    }
  }

  return { issues, prs, readme, contributing };
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
cd worker && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add worker/src/analyzer.ts
git commit -m "feat: add GitHub data fetcher with parallel requests and rate limit handling"
```

---

### Task 6: Anthropic ranker + analyzeRepo orchestrator

**Files:**
- Modify: `worker/src/analyzer.ts` (add `rankIssues` and `analyzeRepo`)

- [ ] **Step 1: Append `rankIssues` and `analyzeRepo` to `worker/src/analyzer.ts`**

Add the following after the `fetchRepoData` function (after its closing brace):

```typescript
interface AnthropicRankedItem {
  number: number;
  difficulty: 'easy' | 'medium' | 'hard';
  reason: string;
  tags: string[];
}

async function rankIssues(
  availableIssues: GitHubIssue[],
  readme: string,
  contributing: string,
  apiKey: string,
): Promise<AnthropicRankedItem[]> {
  // Send the 40 most recent available issues
  const top40 = availableIssues.slice(0, 40);

  const issueList = top40
    .map((i) => `#${i.number}: ${i.title}\n${i.body?.slice(0, 500) ?? '(no body)'}`)
    .join('\n\n---\n\n');

  const prompt = `You are helping open-source contributors find approachable issues to work on. Given the following GitHub issues, rank the top 12 by how contributor-friendly they are for an outside contributor.

README (first 1500 chars):
${readme.slice(0, 1500)}

CONTRIBUTING (first 1000 chars):
${contributing.slice(0, 1000)}

ISSUES:
${issueList}

Return a JSON array of exactly 12 items (or fewer if there are fewer issues), ranked from most to least approachable for a first-time contributor. Each item must follow this exact shape:
{
  "number": <issue number as integer>,
  "difficulty": "easy" | "medium" | "hard",
  "reason": "<1-2 sentences: why this is a good issue to work on and why you assigned this difficulty>",
  "tags": [<one or more of: "bug", "feature", "docs", "test", "refactor", "dx", "perf", "security">]
}

Return the raw JSON array only. No markdown fences, no prose, no explanation before or after the array.`;

  const res = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'x-api-key': apiKey,
      'anthropic-version': '2023-06-01',
    },
    body: JSON.stringify({
      model: 'claude-sonnet-4-5',
      max_tokens: 1024,
      messages: [{ role: 'user', content: prompt }],
    }),
  });

  if (!res.ok) {
    throw new Error(`Anthropic API error: ${res.status}`);
  }

  const data = (await res.json()) as {
    content: { type: string; text: string }[];
  };
  let text = data.content[0]?.text ?? '';

  // Strip accidental markdown fences
  text = text.replace(/^```(?:json)?\s*/m, '').replace(/\s*```$/m, '').trim();

  return JSON.parse(text) as AnthropicRankedItem[];
}

export async function analyzeRepo(
  repo: string,
  env: Env,
): Promise<AnalyzeResponse> {
  const [owner, name] = repo.split('/') as [string, string];

  const { issues, prs, readme, contributing } = await fetchRepoData(owner, name);

  const available = filterAvailableIssues(issues, prs);

  const ranked = await rankIssues(available, readme, contributing, env.ANTHROPIC_API_KEY);

  // Build a lookup map for enriching ranked items with GitHub data
  const issueMap = new Map(issues.map((i) => [i.number, i]));

  const enriched: RankedIssue[] = ranked
    .map((r) => {
      const issue = issueMap.get(r.number);
      if (!issue) return null;
      return {
        number: r.number,
        title: issue.title,
        comments: issue.comments,
        html_url: issue.html_url,
        difficulty: r.difficulty,
        reason: r.reason,
        tags: r.tags,
      } satisfies RankedIssue;
    })
    .filter((r): r is RankedIssue => r !== null);

  return {
    stats: {
      openIssues: issues.length,
      available: available.length,
      openPRs: prs.length,
      ranked: enriched.length,
    },
    issues: enriched,
  };
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
cd worker && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add worker/src/analyzer.ts
git commit -m "feat: add Anthropic ranker and analyzeRepo orchestrator"
```

---

### Task 7: Worker entry point

**Files:**
- Create: `worker/src/index.ts`

- [ ] **Step 1: Create `worker/src/index.ts`**

```typescript
import { getAssetFromKV } from '@cloudflare/kv-asset-handler';
import manifestJSON from '__STATIC_CONTENT_MANIFEST';
import type { Env } from './types';
import { parseRepo } from './parseRepo';
import { analyzeRepo, RateLimitError } from './analyzer';

const assetManifest = JSON.parse(manifestJSON) as Record<string, string>;

const CORS_HEADERS: Record<string, string> = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Methods': 'POST, OPTIONS',
  'Access-Control-Allow-Headers': 'Content-Type',
};

export default {
  async fetch(request: Request, env: Env, ctx: ExecutionContext): Promise<Response> {
    const url = new URL(request.url);

    // CORS preflight
    if (request.method === 'OPTIONS') {
      return new Response(null, { status: 204, headers: CORS_HEADERS });
    }

    // Serve static frontend
    if (url.pathname === '/' && request.method === 'GET') {
      try {
        return await getAssetFromKV(
          { request, waitUntil: ctx.waitUntil.bind(ctx) },
          {
            ASSET_NAMESPACE: env.__STATIC_CONTENT,
            ASSET_MANIFEST: assetManifest,
          },
        );
      } catch {
        return new Response('Not found', { status: 404 });
      }
    }

    // Analysis endpoint
    if (url.pathname === '/api/analyze' && request.method === 'POST') {
      let body: { repo?: unknown };
      try {
        body = (await request.json()) as { repo?: unknown };
      } catch {
        return Response.json(
          { error: 'Invalid JSON body' },
          { status: 400, headers: CORS_HEADERS },
        );
      }

      if (typeof body.repo !== 'string') {
        return Response.json(
          { error: 'Invalid repo format' },
          { status: 400, headers: CORS_HEADERS },
        );
      }

      const repo = parseRepo(body.repo);
      if (!repo) {
        return Response.json(
          { error: 'Invalid repo format' },
          { status: 400, headers: CORS_HEADERS },
        );
      }

      try {
        const result = await analyzeRepo(repo, env);
        return Response.json(result, { headers: CORS_HEADERS });
      } catch (err) {
        if (err instanceof RateLimitError) {
          return Response.json(
            { error: 'GitHub rate limit hit. Wait a minute and try again.' },
            { status: 429, headers: CORS_HEADERS },
          );
        }
        if (err instanceof SyntaxError) {
          return Response.json(
            { error: 'Failed to parse AI response.' },
            { status: 500, headers: CORS_HEADERS },
          );
        }
        console.error('analyzeRepo error:', err);
        return Response.json(
          { error: 'Internal server error.' },
          { status: 500, headers: CORS_HEADERS },
        );
      }
    }

    // 404 for everything else
    return new Response('Not found', { status: 404 });
  },
};
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
cd worker && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add worker/src/index.ts
git commit -m "feat: add Worker entry point with routing, CORS, and error handling"
```

---

### Task 8: Frontend — static/index.html

**Files:**
- Create: `static/index.html`

This is the complete single-file frontend. Write it exactly as specified below.

- [ ] **Step 1: Create `static/index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PRospect — find your next open-source contribution</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #0d1117;
      --bg-card: #161b22;
      --border: #30363d;
      --border-hover: #6e7681;
      --accent: #00ff88;
      --text: #ffffff;
      --muted: #8b949e;
      --very-muted: #6e7681;
      --red: #ff5555;
      --orange: #f0a500;
      --font-mono: 'JetBrains Mono', monospace;
      --font-sans: 'Inter', sans-serif;
    }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: var(--font-sans);
      min-height: 100vh;
      padding-bottom: 64px;
    }

    #progress-bar {
      position: fixed;
      top: 0;
      left: 0;
      height: 2px;
      width: 0;
      background: var(--accent);
      transition: width 2.5s ease-out, opacity 0.4s ease;
      z-index: 1000;
      opacity: 0;
    }

    .container {
      max-width: 860px;
      margin: 0 auto;
      padding: 0 24px;
    }

    /* ── Header ── */
    header {
      padding: 48px 0 40px;
      text-align: left;
    }

    .logo {
      display: flex;
      align-items: baseline;
      gap: 10px;
      margin-bottom: 8px;
    }

    .logo-mark {
      font-family: var(--font-mono);
      font-size: 28px;
      color: var(--accent);
      font-weight: 600;
    }

    .logo-word {
      font-family: var(--font-sans);
      font-size: 26px;
      font-weight: 600;
      color: var(--text);
    }

    .tagline {
      font-size: 14px;
      color: var(--muted);
    }

    /* ── Input area ── */
    .input-area {
      margin-bottom: 40px;
    }

    .input-row {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .prompt-prefix {
      font-family: var(--font-mono);
      font-size: 18px;
      color: var(--accent);
      flex-shrink: 0;
    }

    #repo-input {
      flex: 1;
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 6px;
      color: var(--text);
      font-family: var(--font-mono);
      font-size: 14px;
      padding: 10px 14px;
      outline: none;
      transition: border-color 0.15s;
    }

    #repo-input:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 2px rgba(0, 255, 136, 0.15);
    }

    #repo-input::placeholder {
      color: var(--very-muted);
    }

    #scan-btn {
      font-family: var(--font-mono);
      font-size: 14px;
      font-weight: 600;
      background: var(--accent);
      color: var(--bg);
      border: none;
      border-radius: 6px;
      padding: 10px 20px;
      cursor: pointer;
      white-space: nowrap;
      transition: opacity 0.15s;
      flex-shrink: 0;
    }

    #scan-btn:hover { opacity: 0.85; }
    #scan-btn:disabled { opacity: 0.5; cursor: not-allowed; }

    .input-error {
      margin-top: 8px;
      font-family: var(--font-mono);
      font-size: 13px;
      color: var(--red);
      min-height: 18px;
    }

    .quick-picks {
      margin-top: 14px;
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .quick-pick {
      font-family: var(--font-mono);
      font-size: 12px;
      color: var(--muted);
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 4px 12px;
      cursor: pointer;
      transition: border-color 0.15s, color 0.15s;
    }

    .quick-pick:hover {
      border-color: var(--accent);
      color: var(--accent);
    }

    /* ── Loading ── */
    #loading {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 24px 28px;
      font-family: var(--font-mono);
      font-size: 14px;
      line-height: 2;
    }

    #loading .load-line { color: var(--muted); }

    #loading .load-prefix { color: var(--accent); }

    /* ── Error ── */
    #error-card {
      background: var(--bg-card);
      border-left: 4px solid var(--red);
      border-radius: 8px;
      padding: 16px 20px;
      font-family: var(--font-mono);
      font-size: 13px;
      color: var(--red);
    }

    #error-card strong { font-weight: 600; }

    /* ── Empty ── */
    #empty {
      text-align: center;
      padding: 48px 0;
      color: var(--muted);
      font-size: 14px;
    }

    /* ── Stats bar ── */
    .stats-bar {
      display: flex;
      border: 1px solid var(--border);
      border-radius: 8px;
      overflow: hidden;
      margin-bottom: 20px;
    }

    .stat-block {
      flex: 1;
      padding: 16px 20px;
      border-right: 1px solid var(--border);
      text-align: center;
    }

    .stat-block:last-child { border-right: none; }

    .stat-label {
      font-size: 10px;
      font-family: var(--font-mono);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--muted);
      margin-bottom: 4px;
    }

    .stat-value {
      font-size: 28px;
      font-weight: 600;
      color: var(--text);
      line-height: 1;
    }

    /* ── Filter row ── */
    .filter-row {
      display: flex;
      align-items: center;
      gap: 8px;
      flex-wrap: wrap;
      margin-bottom: 10px;
    }

    .filter-label {
      font-size: 13px;
      color: var(--muted);
    }

    .filter-pill {
      font-family: var(--font-mono);
      font-size: 12px;
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 4px 12px;
      cursor: pointer;
      color: var(--muted);
      transition: border-color 0.15s, color 0.15s;
    }

    .filter-pill.active {
      border-color: var(--accent);
      color: var(--accent);
    }

    .filter-pill:hover:not(.active) {
      border-color: var(--border-hover);
      color: var(--text);
    }

    /* ── Repo notice ── */
    .repo-notice {
      font-size: 12px;
      font-style: italic;
      color: #6e7681;
      margin-bottom: 20px;
      line-height: 1.5;
    }

    /* ── Cards ── */
    #cards-container {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .issue-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 16px 20px;
      display: flex;
      gap: 16px;
      transition: border-color 0.15s;
    }

    .issue-card:hover { border-color: var(--border-hover); }

    .card-rank {
      font-family: var(--font-mono);
      font-size: 14px;
      color: var(--very-muted);
      width: 32px;
      text-align: right;
      flex-shrink: 0;
      padding-top: 2px;
    }

    .card-rank.top3 { color: var(--muted); }

    .card-body { flex: 1; min-width: 0; }

    .card-title {
      font-size: 14px;
      font-weight: 500;
      color: var(--text);
      text-decoration: none;
      display: block;
      margin-bottom: 6px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      transition: color 0.15s;
    }

    .card-title:hover {
      color: var(--accent);
      text-decoration: underline;
    }

    .card-reason {
      font-size: 13px;
      color: var(--muted);
      font-style: italic;
      line-height: 1.6;
      margin-bottom: 10px;
    }

    .card-footer {
      display: flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 6px;
    }

    .badge {
      font-family: var(--font-mono);
      font-size: 11px;
      font-weight: 600;
      border-radius: 20px;
      padding: 2px 8px;
    }

    .badge-easy   { background: #00ff88; color: #0d1117; }
    .badge-medium { background: #f0a500; color: #0d1117; }
    .badge-hard   { background: #ff5555; color: #0d1117; }

    .tag {
      font-family: var(--font-mono);
      font-size: 11px;
      background: #21262d;
      border: 1px solid var(--border);
      color: var(--muted);
      border-radius: 4px;
      padding: 2px 7px;
    }

    .card-meta {
      font-family: var(--font-mono);
      font-size: 12px;
      color: var(--very-muted);
      margin-left: auto;
      white-space: nowrap;
    }

    .hidden { display: none !important; }
  </style>
</head>
<body>
  <div id="progress-bar"></div>

  <div class="container">
    <header>
      <div class="logo">
        <span class="logo-mark">&gt;_</span>
        <span class="logo-word">PRospect</span>
      </div>
      <p class="tagline">find your next open-source contribution</p>
    </header>

    <div class="input-area">
      <div class="input-row">
        <span class="prompt-prefix">$</span>
        <input id="repo-input" type="text" placeholder="owner/repo, HTTPS or SSH URL" autocomplete="off" spellcheck="false">
        <button id="scan-btn">Scan →</button>
      </div>
      <div id="input-error" class="input-error"></div>
      <div class="quick-picks">
        <button class="quick-pick" data-repo="vercel/next.js">vercel/next.js</button>
        <button class="quick-pick" data-repo="django/django">django/django</button>
        <button class="quick-pick" data-repo="astral-sh/uv">astral-sh/uv</button>
        <button class="quick-pick" data-repo="facebook/react">facebook/react</button>
        <button class="quick-pick" data-repo="microsoft/vscode">microsoft/vscode</button>
      </div>
    </div>

    <div id="loading" class="hidden"></div>

    <div id="error-card" class="hidden"></div>

    <div id="empty" class="hidden">No available issues found for this repo.</div>

    <div id="results" class="hidden">
      <div id="stats-bar" class="stats-bar">
        <div class="stat-block">
          <div class="stat-label">Open issues</div>
          <div class="stat-value" id="stat-issues">—</div>
        </div>
        <div class="stat-block">
          <div class="stat-label">Available</div>
          <div class="stat-value" id="stat-available">—</div>
        </div>
        <div class="stat-block">
          <div class="stat-label">Open PRs</div>
          <div class="stat-value" id="stat-prs">—</div>
        </div>
        <div class="stat-block">
          <div class="stat-label">Ranked</div>
          <div class="stat-value" id="stat-ranked">—</div>
        </div>
      </div>

      <div class="filter-row">
        <span class="filter-label">Filter:</span>
        <button class="filter-pill active" data-filter="all">All</button>
        <button class="filter-pill" data-filter="easy" id="filter-easy">Easy</button>
        <button class="filter-pill" data-filter="medium" id="filter-medium">Medium</button>
        <button class="filter-pill" data-filter="hard" id="filter-hard">Hard</button>
      </div>

      <p class="repo-notice">
        Issues with open PRs or active assignees have been filtered out.
        Results reflect Claude's assessment — always check the issue thread before starting work.
      </p>

      <div id="cards-container"></div>
    </div>
  </div>

  <script>
    // ── Helpers ──────────────────────────────────────────────────────────

    function parseRepoInput(input) {
      const trimmed = input.trim();
      if (!trimmed) return null;
      const stripped = trimmed.replace(/\.git$/, '');
      const match = stripped.match(/github\.com[:/]([a-zA-Z0-9_.\-]+\/[a-zA-Z0-9_.\-]+)/);
      if (match && match[1]) {
        const parts = match[1].split('/');
        if (parts[0] && parts[1]) return `${parts[0]}/${parts[1]}`;
      }
      if (/^[a-zA-Z0-9_.\-]+\/[a-zA-Z0-9_.\-]+$/.test(trimmed)) return trimmed;
      return null;
    }

    function esc(str) {
      return String(str)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;');
    }

    // ── State ─────────────────────────────────────────────────────────────

    let allIssues = [];
    let currentFilter = 'all';

    // ── Progress bar ──────────────────────────────────────────────────────

    const progressBar = document.getElementById('progress-bar');

    function startProgress() {
      progressBar.style.transition = 'none';
      progressBar.style.width = '0%';
      progressBar.style.opacity = '1';
      // Trigger reflow so the transition fires from 0
      progressBar.getBoundingClientRect();
      progressBar.style.transition = 'width 2.8s ease-out';
      progressBar.style.width = '85%';
    }

    function finishProgress() {
      progressBar.style.transition = 'width 0.2s ease-out';
      progressBar.style.width = '100%';
      setTimeout(() => {
        progressBar.style.transition = 'opacity 0.4s ease';
        progressBar.style.opacity = '0';
      }, 300);
    }

    // ── Loading state ─────────────────────────────────────────────────────

    const LOADING_LINES = [
      'Fetching issues from GitHub...',
      'Cross-referencing open PRs...',
      'Asking Claude to rank by difficulty...',
    ];

    async function showLoading() {
      const el = document.getElementById('loading');
      el.innerHTML = '';
      el.classList.remove('hidden');
      for (const line of LOADING_LINES) {
        await delay(200);
        const p = document.createElement('p');
        p.className = 'load-line';
        p.innerHTML = `<span class="load-prefix">&gt;</span> ${esc(line)}`;
        el.appendChild(p);
      }
    }

    function hideLoading() {
      document.getElementById('loading').classList.add('hidden');
    }

    function delay(ms) {
      return new Promise(r => setTimeout(r, ms));
    }

    // ── Error / empty states ──────────────────────────────────────────────

    function showError(msg) {
      const el = document.getElementById('error-card');
      el.innerHTML = `<strong>Error:</strong> ${esc(msg)}`;
      el.classList.remove('hidden');
    }

    function hideError() {
      document.getElementById('error-card').classList.add('hidden');
    }

    // ── Results rendering ─────────────────────────────────────────────────

    function renderCards(issues) {
      const container = document.getElementById('cards-container');
      container.innerHTML = '';
      const filtered = currentFilter === 'all'
        ? issues
        : issues.filter(i => i.difficulty === currentFilter);

      filtered.forEach((issue, idx) => {
        const rank = idx + 1;
        const rankClass = rank <= 3 ? 'card-rank top3' : 'card-rank';
        const diffClass = `badge badge-${issue.difficulty}`;
        const tags = (issue.tags || [])
          .map(t => `<span class="tag">${esc(t)}</span>`)
          .join('');

        const card = document.createElement('div');
        card.className = 'issue-card';
        card.dataset.difficulty = issue.difficulty;
        card.innerHTML = `
          <div class="${rankClass}">${rank}</div>
          <div class="card-body">
            <a class="card-title" href="${esc(issue.html_url)}" target="_blank" rel="noopener noreferrer">${esc(issue.title)}</a>
            <p class="card-reason">${esc(issue.reason)}</p>
            <div class="card-footer">
              <span class="${diffClass}">${esc(issue.difficulty)}</span>
              ${tags}
              <span class="card-meta">#${esc(issue.number)} · ${esc(issue.comments)} comment${issue.comments !== 1 ? 's' : ''}</span>
            </div>
          </div>`;
        container.appendChild(card);
      });
    }

    function showResults(data) {
      // Stats
      document.getElementById('stat-issues').textContent = data.stats.openIssues;
      document.getElementById('stat-available').textContent = data.stats.available;
      document.getElementById('stat-prs').textContent = data.stats.openPRs;
      document.getElementById('stat-ranked').textContent = data.stats.ranked;

      // Filter pill counts
      const easyCt = data.issues.filter(i => i.difficulty === 'easy').length;
      const medCt  = data.issues.filter(i => i.difficulty === 'medium').length;
      const hardCt = data.issues.filter(i => i.difficulty === 'hard').length;
      document.getElementById('filter-easy').textContent   = `Easy (${easyCt})`;
      document.getElementById('filter-medium').textContent = `Medium (${medCt})`;
      document.getElementById('filter-hard').textContent   = `Hard (${hardCt})`;

      allIssues = data.issues;
      renderCards(allIssues);
      document.getElementById('results').classList.remove('hidden');
    }

    // ── Filter pills ──────────────────────────────────────────────────────

    document.querySelectorAll('.filter-pill').forEach(pill => {
      pill.addEventListener('click', () => {
        document.querySelectorAll('.filter-pill').forEach(p => p.classList.remove('active'));
        pill.classList.add('active');
        currentFilter = pill.dataset.filter;
        renderCards(allIssues);
      });
    });

    // ── Scan ──────────────────────────────────────────────────────────────

    async function scan() {
      const input = document.getElementById('repo-input').value;
      const errorEl = document.getElementById('input-error');
      const scanBtn = document.getElementById('scan-btn');

      const repo = parseRepoInput(input);
      if (!repo) {
        errorEl.textContent = 'Could not parse a GitHub repo from that input.';
        return;
      }
      errorEl.textContent = '';

      // Reset UI
      hideError();
      document.getElementById('results').classList.add('hidden');
      document.getElementById('empty').classList.add('hidden');
      currentFilter = 'all';
      document.querySelectorAll('.filter-pill').forEach(p => p.classList.remove('active'));
      document.querySelector('[data-filter="all"]').classList.add('active');

      scanBtn.disabled = true;
      startProgress();
      const loadingPromise = showLoading();

      try {
        const res = await fetch('/api/analyze', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ repo }),
        });

        await loadingPromise;
        hideLoading();

        const data = await res.json();

        if (!res.ok) {
          showError(data.error || `HTTP ${res.status}`);
        } else if (!data.issues || data.issues.length === 0) {
          document.getElementById('empty').classList.remove('hidden');
        } else {
          showResults(data);
        }
      } catch (err) {
        await loadingPromise;
        hideLoading();
        showError('Network error — could not reach the server.');
      } finally {
        finishProgress();
        scanBtn.disabled = false;
      }
    }

    // ── Event listeners ───────────────────────────────────────────────────

    document.getElementById('scan-btn').addEventListener('click', scan);

    document.getElementById('repo-input').addEventListener('keydown', e => {
      if (e.key === 'Enter') scan();
    });

    document.querySelectorAll('.quick-pick').forEach(btn => {
      btn.addEventListener('click', () => {
        document.getElementById('repo-input').value = btn.dataset.repo;
        scan();
      });
    });
  </script>
</body>
</html>
```

- [ ] **Step 2: Verify the file exists and is valid HTML**

```bash
wc -c static/index.html
```

Expected: > 10000 bytes.

- [ ] **Step 3: Test locally with wrangler dev**

```bash
cd worker && npx wrangler dev
```

Open `http://localhost:8787` in a browser. Verify:
- Page loads with dark navy background
- Logo `>_` shows in acid-green
- Input area renders with `$` prefix and quick-pick pills
- Clicking a quick-pick populates the input

- [ ] **Step 4: Commit**

```bash
git add static/index.html
git commit -m "feat: add complete frontend — input, loading state, cards, filter, stats"
```

---

### Task 9: GitHub Actions deploy workflow

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: Create `.github/workflows/deploy.yml`**

```yaml
name: Deploy PRospect

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci
        working-directory: worker

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: worker
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: add GitHub Actions deploy workflow via wrangler-action"
```

---

### Task 10: README

**Files:**
- Create: `README.md`

- [ ] **Step 1: Create `README.md`**

```markdown
# PRospect

Find approachable open-source issues to contribute to. Paste any GitHub repo URL or slug, and PRospect fetches open issues, filters out ones already being worked on, then asks Claude to rank the rest by how contributor-friendly they are.

![PRospect screenshot](docs/screenshot.png)

## Setup

1. **Clone the repo**
   ```bash
   git clone https://github.com/your-username/PRospect.git
   cd PRospect
   ```

2. **Install dependencies**
   ```bash
   cd worker && npm install
   ```

3. **Log in to Cloudflare**
   ```bash
   npx wrangler login
   ```

4. **Set the Anthropic API key as a Worker secret**
   ```bash
   npx wrangler secret put ANTHROPIC_API_KEY
   ```
   Paste your key when prompted. It is never stored in any file.

5. **Add Cloudflare API token to GitHub repo secrets**

   In your GitHub repo → Settings → Secrets → Actions, create:
   - `CLOUDFLARE_API_TOKEN` — a Cloudflare API token with "Workers Scripts: Edit" permission

6. **Push to main — GitHub Actions deploys automatically**
   ```bash
   git push origin main
   ```

7. **Your app is live at**
   ```
   https://prospect.<your-subdomain>.workers.dev
   ```

## Local development

```bash
cd worker && npx wrangler dev
```

Open [http://localhost:8787](http://localhost:8787).

## Running tests

```bash
cd worker && npm test
```
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add README with setup instructions"
```

---

## Self-Review

### Spec coverage check

| Spec requirement | Task |
|-----------------|------|
| Worker: GET / serves index.html | Task 7 |
| Worker: POST /api/analyze returns ranked JSON | Task 7 |
| Worker: 404 for all other routes | Task 7 |
| CORS headers on /api/analyze | Task 7 |
| Input normalization (bare slug, HTTPS, SSH, org SSH, trailing paths) | Task 3 |
| Frontend inline validation error in #ff5555 | Task 8 |
| Worker returns 400 for malformed input | Task 7 |
| GitHub: issues + PRs + README + CONTRIBUTING fetched in parallel | Task 5 |
| CONTRIBUTING fallback to docs/CONTRIBUTING.md | Task 5 |
| Filter: no assignees AND not referenced in PR title/body | Task 4 |
| 403/429 from GitHub → 429 response | Task 5 |
| Top 40 issues sent to Claude | Task 6 |
| README ≤ 1500 chars + CONTRIBUTING ≤ 1000 chars in prompt | Task 6 |
| Model: claude-sonnet-4-5, max_tokens: 1024 | Task 6 |
| Response: number, difficulty, reason, tags per issue | Task 6 |
| Strip markdown fences before parsing | Task 6 |
| Parse error → 500 with "Failed to parse AI response." | Task 7 |
| Dark navy bg, acid-green accent, JetBrains Mono + Inter fonts | Task 8 |
| Progress bar + typewriter loading state | Task 8 |
| Stats bar (open issues, available, open PRs, ranked) | Task 8 |
| Filter pills (All / Easy / Medium / Hard) with counts | Task 8 |
| Cards with rank, title link, reason, difficulty badge, tags, meta | Task 8 |
| Quick-pick pills for 5 repos | Task 8 |
| Empty state | Task 8 |
| Error state (red left border, monospace) | Task 8 |
| wrangler.toml with [site] bucket | Task 1 |
| ANTHROPIC_API_KEY never in files | Tasks 1, 2, 6 |
| GitHub Actions deploy on push to main | Task 9 |
| README with setup steps | Task 10 |

All spec requirements are covered.

### Placeholder scan

No TBDs, TODOs, or "similar to task N" references present. All code steps contain full implementation.

### Type consistency

- `GitHubIssue`, `GitHubPR`, `RankedIssue`, `AnalyzeResponse`, `Env` — defined in `types.ts` (Task 2), imported in `filterIssues.ts` (Task 4), `analyzer.ts` (Tasks 5–6), `index.ts` (Task 7). Names are consistent throughout.
- `filterAvailableIssues` — defined in `filterIssues.ts`, called in `analyzer.ts`. Signature matches.
- `analyzeRepo` — defined and exported in `analyzer.ts`, imported in `index.ts`. Returns `AnalyzeResponse`.
- `parseRepo` — defined and exported in `parseRepo.ts`, imported in `index.ts`. Returns `string | null`.
- `RateLimitError` — defined and exported in `analyzer.ts`, caught by `instanceof` in `index.ts`.
- `AnthropicRankedItem` — local to `analyzer.ts`, not exported (not needed elsewhere).

No inconsistencies found.
